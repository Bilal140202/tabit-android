<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose handles dark theming; this is only for splash screen / non-Compose contexts -->
    <style name="Theme.Tabit" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
